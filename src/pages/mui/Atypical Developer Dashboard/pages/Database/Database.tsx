import {FC} from "react";
import {Grid} from "@mui/material";

const ADDatabase:FC = ()=>{
    return (
        <Grid item xs={8} >
            This is database page.
        </Grid>
    )
}

export default ADDatabase;